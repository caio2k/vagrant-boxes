{
  "min_packer_version": "1.3.0",
  "variables": {
    "cores": "2",
    "memory": "512",
    "disk_size": "10140",
    "ssh_user": "vagrant",
    "ssh_pass": "vagrant",
    "ssh_wait_timeout": "10000s",
    "debian_version": "8.11.0",
    "iso_checksum": "28641eb3f657a71fccc1506b2953b4f3130937cdcff686162a882bc045ad32482ec4b5eb3d2d711d59cdd610e338579776e96a11895d51bbe88e831fa3d8a7af",
    "vm_name": "devel"
  },
  "builders": [
    {
      "boot_command": [
        "<esc><wait>",
        "install <wait>",
        "preseed/url=http://{{ .HTTPIP }}:{{ .HTTPPort }}/debian8.cfg <wait>",
        "debian-installer=en_US <wait>",
        "auto <wait>",
        "locale=en_US <wait>",
        "kbd-chooser/method=us <wait>",
        "netcfg/get_hostname={{ .Name }} <wait>",
        "netcfg/get_domain=vagrantup.com <wait>",
        "fb=false <wait>",
        "debconf/frontend=noninteractive <wait>",
        "console-setup/ask_detect=false <wait>",
        "console-keymaps-at/keymap=us <wait>",
        "keyboard-configuration/xkb-keymap=us <wait>",
        "passwd/root-login=false <wait>",
        "passwd/user-fullname={{user `ssh_user`}} <wait>",
        "passwd/username={{user `ssh_user`}} <wait>",
        "passwd/user-password=\"{{user `ssh_pass`}}\" <wait>",
        "passwd/user-password-again=\"{{user `ssh_pass`}}\" <wait>",
        "<enter><wait>"
      ],
      "boot_wait": "10s",
      "headless": false,
      "disk_size": "{{user `disk_size`}}",
      "guest_additions_path": "VBoxGuestAdditions_{{.Version}}.iso",
      "guest_os_type": "Debian_64",
      "http_directory": "http",
      "iso_checksum_type": "sha512",
      "iso_checksum": "{{user `iso_checksum`}}",
      "iso_url": "https://cdimage.debian.org/mirror/cdimage/archive/{{user `debian_version`}}/amd64/iso-cd/debian-{{user `debian_version`}}-amd64-netinst.iso",
      "shutdown_command": "echo '{{user `ssh_pass`}}' | sudo -S /sbin/shutdown -hP now",
      "ssh_username": "{{user `ssh_user`}}",
      "ssh_password": "{{user `ssh_pass`}}",
      "ssh_port": 22,
      "ssh_wait_timeout": "{{user `ssh_wait_timeout`}}",
      "type": "virtualbox-iso",
      "vm_name": "debian-{{user `debian_version`}}-{{user `vm_name`}}",
      "vboxmanage": [
        ["modifyvm", "{{.Name}}", "--cpus", "{{user `cores`}}"],
        ["modifyvm", "{{.Name}}", "--memory", "{{user `memory`}}"],
        ["modifyvm", "{{.Name}}", "--chipset", "ich9"],
        ["modifyvm", "{{.Name}}", "--paravirtprovider", "kvm"],
        ["modifyvm", "{{.Name}}", "--nictype1", "virtio"]
      ]
    }
  ],
  "provisioners": [
    {
      "type": "shell",
      "execute_command": "echo '{{user `ssh_pass`}}' | {{.Vars}} sudo -E -S bash '{{.Path}}'",
      "scripts": [
        "scripts/base.sh",
        "scripts/vmtool.sh",
        "scripts/vagrant.sh",
        "scripts/devel.sh",
        "scripts/ansible.sh",
        "scripts/cleanup.sh"
      ]
    }
  ],
  "post-processors": [
    [{
      "type": "vagrant",
      "compression_level": 9,
      "output": "./boxes/debian-{{user `debian_version`}}-{{user `vm_name`}}-vbox.box",
      "keep_input_artifact": false
    },
    {
      "type": "vagrant-cloud",
      "box_tag": "caio2k/debian8",
      "access_token": "{{user `cloud_token`}}",
      "version": "{{user `debian_version`}}-0",
      "no_release": "true"
    }]
  ]
}

